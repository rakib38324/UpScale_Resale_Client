import React from 'react';
import Loading from '../Shared/Loading/Loading';

const MyOrder = () => {
    return (
        <div>
            <Loading></Loading>
        </div>
    );
};

export default MyOrder;